'use strict';

const bookings = [];

const createBooking = function(flightNumber, numPassengers, price) {

  const booking = {
    flightNum,
    numPassengers,
    price
  }
  bookings.push(booking);
}